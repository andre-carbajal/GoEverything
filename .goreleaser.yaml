version: 2

project_name: goeverything

before:
  hooks:
    - go mod tidy
    - go vet ./...

builds:
  - id: ge-darwin
    main: ./cmd/ge
    binary: ge
    ldflags:
      - -s -w
    env:
      - CGO_ENABLED=1
    goos:
      - darwin
    goarch:
      - amd64
      - arm64
  - id: ge-windows
    main: ./cmd/ge
    binary: ge
    ldflags:
      - -s -w
    env:
      - CGO_ENABLED=0
    goos:
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - formats: [tar.gz]
    builds:
      - ge-darwin
      - ge-windows
    format_overrides:
      - goos: windows
        formats: [zip]
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}

checksum:
  name_template: checksums.txt

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"

brews:
  - name: goeverything
    directory: Formula
    repository:
      owner: andre-carbajal
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    homepage: https://github.com/andre-carbajal/GoEverything
    description: Fast local file indexing and search
    license: MIT
    test: |
      system "#{bin}/ge", "--help"
